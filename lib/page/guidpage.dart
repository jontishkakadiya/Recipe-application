import 'package:flutter/material.dart';

class GuidPage extends StatelessWidget {
  const GuidPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Guid Screen'),
    );
  }
}
