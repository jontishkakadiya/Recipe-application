import 'package:flutter/material.dart';

class MyBagPage extends StatelessWidget {
  const MyBagPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('my bag screen'),
    );
  }
}
